parameters:
  name: 'check'

jobs:
- job: ${{ parameters.name }}
  displayName: check
  steps:
    - template: install.yaml
    - script: cargo fetch
      displayName: cargo fetch
    - script: cargo check
      displayName: cargo check
